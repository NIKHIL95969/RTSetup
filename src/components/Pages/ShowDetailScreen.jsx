import React, { useState, useEffect } from 'react';
import ShowDetail from './ShowDetails';

const ShowDetailScreen = ({ match }) => {

  return <ShowDetail show={show} />;
};

export default ShowDetailScreen;
